Exercices - 05_scrapy

1) Spider local
- Ajouter un champ url dans l item.
- Extraire aussi un resume dans un <p>.

2) Request/Response
- Construire une Response avec un HTML local.
- Extraire tous les liens et les afficher.

3) Pipeline
- Modifier le pipeline pour transformer le titre en lowercase.
- Rejeter les titres trop courts (< 3 chars).

4) CrawlerProcess
- Executer un spider qui parse example.com et renvoie title + url.

5) ItemLoader
- Utiliser un loader pour nettoyer les espaces.

6) Export JSON
- Exporter la liste d items dans un fichier JSON.

